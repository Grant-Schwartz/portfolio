import { Flex, Heading, Text } from "@chakra-ui/react";
import { FC } from "react";


export const Nav: FC = () => {
    return (
        <Flex margin="20px">
            <Heading>& grant schwartz</Heading>
        </Flex>
    );
}